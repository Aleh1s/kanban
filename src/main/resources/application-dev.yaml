spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskmate
    username: postgres
    password: 123
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
  config:
    import: secrets.yaml